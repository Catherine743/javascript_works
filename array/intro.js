 /*

 array

 If we have to store and organize multiple objects with same or different type -object,set

 keeps order
 duplicates
 mutable (can update in array but not string after creation)

 set:
 keeps not order 
 duplicates not allowed;
 mutable

 */

//   var colors = ["red","blue", "black","black"]

//   console.log(colors);
//   console.log(colors[0]);
//   console.log(colors[0],colors[1]);









  
  

  
  
