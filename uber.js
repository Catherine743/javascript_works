// template literal

// syntax (`message to be print ${variableName}`)


var code = 7405;
var characters = "qlrnn4a1sbt";

console.log(`your code ${code} never share this code with anyone ${characters}`);
